<li class="page-item <%= 'disabled' if current_page.last? %>">
  <%= link_to_unless current_page.last?, raw(t 'views.pagination.last'), url, remote: remote, class: 'page-link' do %>
    <span class="page-link"><%= raw(t 'views.pagination.last') %></span>
  <% end %>
</li>
