<%# トップバー %>
<header class="top-bar">
  <h1 class="fs-5 m-0"><%= yield :page_title %></h1>
  <div class="d-flex align-items-center gap-4">
    <%# 新規作成ボタン %>
    <%= link_to "新規作成", new_expense_report_path, class: "btn btn-primary-custom" %>
    <%# アイコン %>
    <div class="avatar">
      <%= current_user.name[0] %>
    </div>
  </div>
</header>